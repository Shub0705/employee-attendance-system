apiVersion: v1
kind: Service
metadata:
  name: f-service
  namespace: posh
spec:
  selector: 
    app: em1
  type: NodePort
  ports:
   - protocol: TCP
     port: 80
     targetPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: b-service
  namespace: posh
spec:
  selector:
    app: em2
  type: NodePort
  ports:
   - protocol: TCP
     port: 5000
     targetPort: 5000
