apiVersion: v1
kind: Service
metadata:
  name: Employe-frontend
  namespace: job
spec:
  selector:
    global: logic
  ports:
   - protocol: TCP
     port: 80
     targetPort: 80

---

piVersion: v1
kind: Service
metadata:
  name: Employe-bacend
  namespace: job
spec:
  selector:
    global: logic-b
  ports:
   - protocol: TCP
     port: 5000
     targetPort: 5000
